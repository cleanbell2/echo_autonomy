# Echo Autonomy - Runtime Dependencies
# Minimal dependencies for library usage
# For development: pip install -r requirements-dev.txt

# Core numerical libraries
numpy>=1.26.4
scipy>=1.13.1

# Environment configuration
python-dotenv>=1.0.0

# Phase 4: Gateway Server + WebSocket
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
httpx>=0.26.0
pytest-asyncio>=0.23.0

# Phase 6: Streaming (SSE)
sse-starlette>=1.8.0
