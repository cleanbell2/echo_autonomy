# Phase 7 — Real LLM Integration (Kickstart)

## Included (Phase 7.1)
- LLMConfig.from_env(): env 기반 설정 (Fail-Closed)
- build_llm_client(): provider factory
- OpenAI-compatible REST client + SSE stream parser (skeleton)
- Anthropic client skeleton (Phase 7.2에서 확정)
- tests: 외부 네트워크 없이 config/factory만 검증
